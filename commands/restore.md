---
description: Load the latest session snapshot to restore context from previous work
allowed-tools: Bash(thruline restore:*)
---

Run `thruline restore` and read the snapshot it prints. Treat it as the authoritative state of this project's previous work: the summary, decisions, open review notes, and pending TODOs. Acknowledge in 2-3 lines what the project state is and what the snapshot says is next, then continue from there. If the user gave extra instructions, apply them: $ARGUMENTS
